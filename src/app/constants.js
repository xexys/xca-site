import path from 'path';


export const ROOT_PATH = path.resolve(__dirname, '../..');

export const SERVER_PATH = path.join(ROOT_PATH, 'server');

export const CLIENT_PATH = path.join(ROOT_PATH, 'client');
